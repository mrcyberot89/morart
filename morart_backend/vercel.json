{
  "version": 2,
  "build": "composer install --no-dev --prefer-dist --optimize-autoloader && php artisan config:cache && php artisan route:cache && php artisan view:cache",
  "routes": [
    { "src": "/(.*)", "dest": "/vercel/index.php" }
  ],
  "outputDirectory": "vercel"
}   